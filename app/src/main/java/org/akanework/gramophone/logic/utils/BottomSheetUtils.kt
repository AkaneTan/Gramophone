package org.akanework.gramophone.logic.utils

object BottomSheetUtils {
    enum class BottomFrameManipulateState {
        HIDE_SHEET, SHOW_SHEET, HIDE_NAV, SHOW_NAV, HIDE_ALL, SHOW_ALL
    }
    enum class ComponentState {
        SHOWN, HIDDEN
    }
}